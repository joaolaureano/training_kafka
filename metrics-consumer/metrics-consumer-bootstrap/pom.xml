<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.joaolaureano.trainingkafka</groupId>
    <artifactId>metrics-consumer</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>metrics-consumer-bootstrap</artifactId>
  <name>metrics-consumer :: bootstrap</name>

  <!--
    A raiz de composição. É o ÚNICO módulo que enxerga os três outros ao mesmo
    tempo — e é justamente por isso que ele consegue ligar o caso de uso ao
    adapter sem que nenhum dos dois saiba da existência do outro.
  -->
  <dependencies>
    <dependency>
      <groupId>dev.joaolaureano.trainingkafka</groupId>
      <artifactId>metrics-consumer-adapters</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>dev.joaolaureano.trainingkafka</groupId>
      <artifactId>metrics-consumer-application</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>dev.joaolaureano.trainingkafka</groupId>
      <artifactId>metrics-consumer-domain</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals><goal>repackage</goal></goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
